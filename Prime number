#include<iostream>
using namespace std;
int c = 0;
void checkPrime(int n) {

	for(int i = 2; i < n/2; i++) {
		if(n % 2 == 0) {
			c = 1;
			break;
		}
	}
	if(c==0)
	cout << "Prime";
	else
	cout<<"Not Prime";
}

int main() {
	int n;
	cin>>n;
	checkPrime(n);
	return 0;
}
